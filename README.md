# Diagnose
Bash script to verify WordPress in docker environment

# Before run 

sudo apt update && sudo apt install -y sysstat procps iproute2 curl util-linux

# Run 
chmod + x diagnose.sh && ./diagnose.sh